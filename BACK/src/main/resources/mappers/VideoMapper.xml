<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ssafy.ssafit.model.dao.VideoDao">
  
  	<select id="selectAll" resultType="Video">
  		SELECT * 
  		FROM video
  	</select>
  
  	<select id="selectOne" parameterType="int" resultType="Video">
  		SELECT *
  		FROM video
  		WHERE video_id = #{id}
  	</select>
  	
  	<select id="search" resultType="Video" parameterType="SearchCondition">
		SELECT *
		FROM video
		<if test="key != 'none'">
			WHERE ${key} LIKE concat('%', #{word}, '%')
		</if>
		<if test="orderBy != 'none'">
			ORDER BY ${orderBy} ${orderByDir}
		</if>
	</select>
	
	<insert id="insert" parameterType="Video">
  		INSERT INTO video (title, url, channel_name, part)
  		VALUES (#{title}, #{url}, #{channel_name}, #{part})
  	</insert>
  
  
  	<update id="update" parameterType="Video">
  		UPDATE video
  		SET title= #{title}, url = #{url}, channel_name = #{channel_name}, part = #{part}
  		WHERE video_id = #{video_id};
  	</update>
  
  
	<delete id="delete" parameterType="int">
		DELETE FROM video
		WHERE video_id = #{id};
	</delete>
  	
  </mapper>